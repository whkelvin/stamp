datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
}

model posts {
  post_id String @id @default("")
  link String @default("")
  title String @default("")
  description String @default("")
  created_date DateTime @default(now())
  root_domain String @default("")
}

